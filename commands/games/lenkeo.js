module.exports = {
    name: 'lenkeo',
    aliases: [],
    category: 'games',
    utilisation: '{prefix}lenkeo',

    execute(client, message) {
       var member = {
           id: message.author.id,
           guildId: message.guild.id,
       }
       
      
    },
}